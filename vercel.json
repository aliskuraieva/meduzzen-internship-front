{
  "builds": [
    {
      "src": "dist/meduzzen-internship-front/**/*",
      "use": "@vercel/static"
    }
  ],
  "env": {
    "NPM_CONFIG_PREFIX": "/vercel/.npm"
  },
  "buildCommand": "npm install && ng build --configuration production"
}
